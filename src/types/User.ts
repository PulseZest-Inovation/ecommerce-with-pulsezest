export interface UserType {
  id: string;
  email: string;
  phoneNumber: string;
  fullName: string;
  roleType: string;
  createdAt?: any;
  applicationId?: string; // Optional field for application ID
};

